{
  "name": "api-auth",
  "type": "commonjs",
  "scripts": {
    "build": "nest build -p tsconfig.app.json -b swc --type-check .",
    "dev": "nest start -p tsconfig.app.json -b swc --type-check . --watch"
  }
}
