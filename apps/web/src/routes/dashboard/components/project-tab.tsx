import { component$ } from '@builder.io/qwik';
import { Box } from '@producktivity/ui';

export const ProjectTab = component$(() => {
  return (
    <Box width="full">
      <Box padding="2">project tab</Box>
    </Box>
  );
});
