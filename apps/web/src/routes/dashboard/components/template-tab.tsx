import { component$ } from '@builder.io/qwik';
import { Box } from '@producktivity/ui';

export const TemplateTab = component$(() => {
  return (
    <Box width="full">
      <Box padding="2">template tab</Box>
    </Box>
  );
});
