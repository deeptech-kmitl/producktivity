import { component$ } from '@builder.io/qwik';
import { Box } from '@producktivity/ui';

export const Credit = component$(() => {
  return (
    <Box width="full" variant="surface">
      <Box padding="2">Credit</Box>
    </Box>
  );
});
